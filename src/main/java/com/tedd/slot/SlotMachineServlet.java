package com.tedd.slot; import javax.servlet.*; import javax.servlet.http.*; import java.io.*; public class SlotMachineServlet extends HttpServlet{ private final SlotEngine engine=new SlotEngine(); protected void doGet(HttpServletRequest req,HttpServletResponse resp)throws IOException{ resp.setContentType("application/json"); resp.getWriter().write("{\"result\": \""+engine.spin()+"\"}"); }}